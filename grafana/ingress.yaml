apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-ingress
  annotations:
    kubernetes.io/ingress.class: "grafana" # ระบุว่าใช้ Ingress Controller ตัวไหน
    #spec.ingressClassName: "jenkins"
spec:
  rules:
  - host: my-grafana.local  # Domain ที่เราต้องการใช้งาน
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: grafana-service # ชื่อ Service ที่เราสร้างไว้ข้างบน
            port:
              number: 8000